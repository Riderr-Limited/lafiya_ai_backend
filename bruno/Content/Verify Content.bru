meta {
  name: Verify Content
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/api/content/{{contentId}}/verify
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "trustScore": 90
  }
}
